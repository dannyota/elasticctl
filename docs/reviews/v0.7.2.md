# v0.7.2 development review

Independent code, evidence, and privacy reviews passed with no remaining
findings. All workspace gates, thirty-three matrix checks, both product-client
checks, and the nonpublishing preflight passed.

The runtime is unchanged from v0.7.1. The independent 0.7.0 and 0.7.1
whole-branch reviews stand for that code. This review covers the new tests,
controller registration, report validator, and measured evidence. See the
[live findings](../conformance/v0.7.2/findings.md) for coverage and cleanup.

## Review corrections

| Finding | Resolution |
| --- | --- |
| SDK admission checks covered only one default query name and used a raw client for the opt-in catalog | Check both exact unknown-tool errors and the two-tool catalog delta through rmcp sessions. |
| The legacy SDK check depended on the SDK's default revision | Explicitly request and assert negotiated `2025-11-25`. |
| The cancellation check sampled output before later requests and ignored malformed frames | Inspect the complete captured stream after the usability check and shutdown; reject partial or invalid frames and any cancelled request id. |
| The first live matrix rejected the current protocol's `resultType` field | Validate and remove the discriminator once at the live harness receive boundary; keep the strict payload validators. |
| The new process regression allowed missing tool-error fields | Require the exact normalized result keys. |

The controller gives its confirmed Fleet setup marker to every contract
requiring Fleet policies. The eleventh contract retains the reviewed fixture
provisioning, ownership, cleanup, and mutation boundaries. Both changes
received independent review. The JSON Schema validator is a pinned MCP dev
dependency with default features disabled; the license allowlist is unchanged.

## Verification

The measured source commit is
`d0c79b4cc99d0e8ae5ae15b476a7f5eb9e1eb4f2`. The workspace passed 1,702 tests
across 70 suites, with zero failures and eleven ignored live tests. This
includes 154 MCP tests and 104 offline live-harness tests. The final matrix
ran all eleven live contracts explicitly on each flavor.

| Check | Result |
| --- | --- |
| `cargo fmt --all --check` | Pass |
| `cargo clippy --locked --workspace --all-targets --jobs 2 -- -D warnings` | Pass |
| `cargo test --locked --workspace --jobs 2 -- --test-threads=4` | Pass |
| `cargo deny check advisories bans licenses sources` | Pass |
| `./scripts/check-packages.sh` | Pass |
| `./scripts/check-fixtures.sh` | Pass |
| `./scripts/check-conformance-reports.sh` | Pass |
| `cargo publish --workspace --dry-run --locked --registry crates-io` | Pass |
| `git diff --check` | Pass |

The publish dry run packaged and verified all four crates from a clean
checkout of the measured source commit. Every upload was aborted by dry-run
mode. The later documentation commit records that evidence; it does not
imply that the full Rust checks ran again on the documentation commit.

## Evidence limits

The Task 3 worker retained summaries of its original RED/GREEN commands,
not raw command output. The primary retained a later controlled baseline
comparison: removing only the receive hook makes the actual-child regression
fail with the observed catalog-field error. That is a baseline comparison,
not reconstructed original RED evidence. Primary command logs also retain the
restored code's passing offline tests.

The first matrix had three MCP failures with clean cleanup and no MCP tool
read evidence. All three flavors were rerun after the reviewed harness fix.
The final reports contain eleven passes each. No runtime fix followed that
matrix.

Both product clients negotiated `2025-11-25`; current protocol live proof uses
the project subprocess harness. Their absent-query checks stopped at catalog
refusal. Both sent the invalid list input and received a server error. See the
findings for the live HTTP capture limit and the closed-alert cleanup allowance.

Package versions remain 0.7.1. No v0.7.2 tag, release, or publication ran.
Release and publication require separate approval under
[releasing.md](../releasing.md) and the MCP design.
