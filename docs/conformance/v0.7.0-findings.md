# elasticctl 0.7.0 MCP findings

All seven tools passed on all three deployment flavors. MCP Inspector also
passed initialization, catalog discovery and all seven calls on the
self-managed lab. No live check failed or skipped. Cleanup passed.

| Flavor | Version | Tool reads | Baseline cleanup |
| --- | --- | --- | --- |
| Serverless | 9.6.0 | 7 passed | Verified |
| Elastic Cloud Hosted | 9.5.2 | 7 passed | Verified |
| Self-managed | 9.5.1 | 7 passed | Verified |

The run completed on 2026-09-06 (UTC+07:00) from development commit `a889b19`.
Package versions remain 0.6.2. Each flavor ran the actual CLI stdio server
through `mcp_reads_foundation_tools_without_residue`. The harness prepared a
disabled rule, exception container and item through the existing CLI, then
read them through MCP. It created no event or index.

## Tool coverage

| Tool | Serverless | Hosted | Self-managed | Inspector on self-managed |
| --- | --- | --- | --- | --- |
| `exceptions_get` | Pass | Pass | Pass | Pass |
| `exceptions_list` | Pass | Pass | Pass | Pass |
| `rules_get` | Pass | Pass | Pass | Pass |
| `rules_list` | Pass | Pass | Pass | Pass |
| `rules_prebuilt_status` | Pass | Pass | Pass | Pass |
| `stack_doctor` | Pass | Pass | Pass | Pass |
| `stack_info` | Pass | Pass | Pass | Pass |

Each smoke proved the exact one-page catalog, nonempty owned results,
selected fields, page metadata, matching text and structured content, and
child shutdown within one session.

## Protocol and client

Offline raw-wire and real-process tests passed for current protocol
`2026-07-28` and legacy initialization with `2025-11-25`. The live smoke used
the legacy handshake. Current protocol support was proved offline.

The real client was MCP Inspector CLI (`@modelcontextprotocol/inspector`)
2.5.0 on Node 24.20.0. It negotiated `2025-11-25`, listed the exact catalog,
and called every tool on a separate owned lab fixture. Inspector runs one
method per process, so these calls used separate connections. Its Cloud
`stack_info` calls supplied the numeric versions; the raw smoke proved all
seven tools on each Cloud flavor. Client settings were unchanged.

## Cleanup and evidence limits

Every smoke deleted its exact owned objects and restored its captured
baseline. Prebuilt rule counts were unchanged, and no marker rules,
exception lists or indices remained. Inspector's separate rule and exception
container were deleted, and its marker audits were empty. Lab teardown
succeeded; separate checks found zero lab containers and volumes. Hosted
remained running.

Raw target output remains in private ignored logs. This document records
safe versions, tool and protocol names, outcomes and cleanup facts. The older
ten-contract matrix was not rerun for this proof. The integrated MCP matrix
contract and broader client coverage remain in 0.7.2.
