# elasticctl 0.6.2 Fleet Conformance Implementation Plan

> **For agentic workers:** Use superpowers:subagent-driven-development or superpowers:executing-plans to implement each task. The coordinator owns Git operations and verification.

**Goal:** Prove Fleet policy transfer on all three deployment flavors and close the deferred integration-policy selector defect.

**Architecture:** Keep policy behavior in the existing API orchestration. Add shared, private Fleet baseline reads to the test-support crate, then extend the existing live test and conformance runner. Fleet cleanup uses nonce ownership and one-shot mutations.

**Tech Stack:** Rust 2024, Tokio, wiremock, assert_cmd, xtask, Bash/jq, Podman.

**Spec:** [Fleet design](../specs/elasticctl-fleet-design.md), especially sections 9 and 14; [shared design](../specs/elasticctl-design.md), especially section 8.3.

## Global Constraints

- The tenth contract is `fleet_transfers_agent_and_integration_policies_without_residue`, registered as `fleet` with `features: &[FLEET_POLICIES]`.
- Preserve the 9.5.1 feature floor, existing output shapes, and `cli -> api -> core` dependency direction.
- Every created policy id and name starts with `elasticctl-live-`. Each run adds a nonce to its policy descriptions and uses empty agent monitoring.
- Register exact ids and allowed normalized policy states before creating or updating them. Cleanup rechecks nonce, id, exact name, namespace, active space, allowed description, portable configuration, parent ids, package coordinate, agents, and attachments before each one-shot mutation. Never send `force` or replay an ambiguous mutation blindly.
- Delete integrations before parents. A remaining integration blocks parent cleanup and package uninstall.
- Install exact `system` only when absent, using the status response's nonempty `latestVersion`. Reserve a cleanup lease before `post_once`, then distinguish a confirmed install from an unconfirmed attempt. Only a confirmed owned version may be removed with `delete_once`. Pre-existing packages are never removed.
- Machine report keys are exactly `flavor`, `version`, `contracts`, `contract`, `result`, and `error_class`. Public narrative findings may describe behavior and aggregate cleanup results, but contain no credentials, target hosts or URLs, account/deployment/object identities, user data, or raw failures. Raw output stays in ignored private logs. Scan both JSON and new narrative evidence before committing.
- Keep the Hosted deployment running. Read live configuration only inside command processes from the main checkout. Never print or copy `.env`.
- Update the spec before changing behavior. Remove a closed item from the current backlog. Do not edit recorded fixtures by hand.
- Local builds use `--jobs 2`; tests use `--test-threads=4`. Run one build-heavy command at a time.
- Keep versions at 0.6.1 during development. Release preparation, tagging, and crates.io publishing are separate steps.

## Files and order

| Task | Owned files | Deliverable |
| --- | --- | --- |
| 1 | `crates/elasticctl-api/src/fleet/integration_policy_ops.rs`, `crates/elasticctl-api/tests/integration_policies.rs`, `docs/specs/elasticctl-fleet-design.md`, `docs/plans/v0.6.1.md` | Checked read-only selectors and regressions |
| 2 | `crates/elasticctl-api/tests/support/fleet.rs`, `crates/elasticctl-api/tests/support/mod.rs`, `crates/elasticctl-api/tests/support/Cargo.toml`, `xtask/Cargo.toml`, `Cargo.lock`, `xtask/src/conformance.rs`, `scripts/check-conformance-reports.sh`, `docs/specs/elasticctl-design.md` | Shared Fleet audits and tenth-contract registration |
| 3 | `crates/elasticctl-cli/tests/live/fleet.rs`, `crates/elasticctl-cli/tests/live/fleet/cleanup.rs`, `crates/elasticctl-cli/tests/live.rs`, `docs/specs/elasticctl-fleet-design.md` | Fleet lifecycle, ownership cleanup, and offline safety proofs |
| 4 | `docs/conformance/v0.6.2/*.json`, `docs/conformance/v0.6.2/findings.md`, `docs/reviews/v0.6.2.md`, both design specs, `CHANGELOG.md`, this plan | Reviewed implementation and measured matrix |

Tasks run in this order, with independent Sol review before the next task depends on the result. Terra owns implementation. Workers make no Git or live-target changes; the coordinator owns those operations.

### Task 1: Check integration-policy read identity

**Interface:** Preserve `resolve`, `get`, and `export` signatures. A successful exact-id read must echo the requested id. A fresh read selected from a list must produce the same normalized `IntegrationPolicySummary` as that row. Apply this to explicit name resolution and `--all-custom` export.

- [x] Update Fleet section 9: mismatched ids and inconsistent summaries are malformed `http` responses. Error text describes the inconsistency without values from the response.
- [x] Add public get/export regressions for wrong exact id, wrong fetched name, namespace, parent ids, package coordinate, and description after name selection. Cover `--all-custom` and a consistent success case. Compare parent ids in sorted order without changing public output ordering or accepting duplicates.

```rust
let error = integration_policy_ops::get_op(&transport, "integration-1")
    .await.expect_err("a different returned id must fail");
assert_eq!(error.kind, ErrorKind::Http);
```

- [x] Run the new tests before the fix and record their expected failures.
- [x] Introduce a private checked-read helper that decodes the fetched summary, checks the requested id, and optionally compares a list summary. Return the fetched item and summary together. Use it before any parent/package reads or export encoding. Preserve mutation-specific error handling.
- [x] Run `cargo test --locked -p elasticctl-api --test integration_policies --jobs 2 -- --test-threads=4` and formatting.
- [x] Remove the resolved deferred item from the 0.6.1 plan. Review and commit the task.

### Task 2: Audit Fleet state in the conformance controller

**Interface:** Add a private tooling module exported as `elasticctl_api_test_support::fleet`. It depends only on core transport and JSON, avoiding an API dev-dependency cycle.

```rust
pub type PackageInventory = std::collections::BTreeMap<String, String>;
#[derive(Clone, Debug, PartialEq, Eq)]
pub struct FleetState {
    pub agent_policies: std::collections::BTreeSet<String>,
    pub integration_policies: std::collections::BTreeSet<String>,
    pub packages: PackageInventory,
}
impl FleetState {
    pub async fn capture(t: &elasticctl_core::Transport)
        -> elasticctl_core::Result<Self>;
    pub fn markers_empty(&self) -> bool;
}
pub async fn installed_packages(t: &elasticctl_core::Transport)
    -> elasticctl_core::Result<PackageInventory>;
```

- [ ] Add offline tests for malformed inventory, duplicate package names, short reads, invalid cursors, reordered inventory, paging contradictions, marker ids with ordinary names, and ordinary ids with marker names for both policy kinds. Use wiremock responses as adversarial tests, never as recorded fixtures.
- [ ] Read installed packages with `/api/fleet/epm/packages/installed?perPage=1000&sortOrder=asc`. Reject every item with missing or non-`installed` status; never filter those rows. Retain `(name, version)` pairs after validation. Follow the recorder's strict one-page envelope validation, including `total <= 1000`, exact count, and at most two scalar `searchAfter` values.
- [ ] Read policies by the existing `created_at` paging contract, with simplified integrations. Validate page, per-page, total stability, ids, names, duplicates, and completeness. Retain only ids whose id or name starts with `elasticctl-live-`; discard all other payload data.
- [ ] Add the private support crate as an xtask dependency. Register `FLEET_POLICIES` and the tenth contract. Before baseline capture, call Fleet setup once when the feature is supported. Only after validated setup, pass `ELASTICCTL_CONFORMANCE_FLEET_SETUP=1` to contract children. Standalone live Fleet execution calls setup only when this private flag is absent. Do not call Fleet routes below its floor.
- [ ] Add `Option<FleetState>` to `TargetState`. Require no Fleet markers at start and after every contract; require the exact baseline package inventory. Unsupported Fleet uses `None`, preserving explicit capability skips.

```rust
assert_eq!(CONTRACTS[9].name, "fleet");
assert_eq!(CONTRACTS[9].features[0].feature, Feature::FleetPolicies);
```

- [ ] Add controller regressions showing each Fleet marker and package drift prevents report publication. Prove a below-floor capture uses `None` and sends zero `/api/fleet/*` requests. Prove a supported session sends exactly one setup before its first Fleet GET; child execution with the private setup flag sends no second setup. Extend the shell validator with the ten-name v0.6 contract list while retaining historical lists.
- [ ] Update shared design section 8.3 to describe ten contracts, gated Fleet baseline reads, setup, exact inventory audits, and private tooling reuse. Do not claim live measurement yet.
- [ ] Run the support tests, `cargo test --locked -p xtask conformance --jobs 2 -- --test-threads=4`, and the report validator. Review and commit.

### Task 3: Exercise Fleet transfer and prove cleanup safety

**Interface:** Add `#[path = "live/fleet.rs"] mod fleet;` under `tests/live.rs`. The explicit path avoids the existing `tests/fleet.rs` integration target. The root ignored test keeps the exact contract name and delegates to the module. `live/fleet.rs` owns the lifecycle and CLI assertions; its `cleanup` child owns package leases, policy ownership, cleanup, and safety tests. Both files belong to this task. They can use parent CLI helpers and the shared Fleet audit types. Keep the existing live tests' cleanup behavior intact.

- [ ] Build a Fleet cleanup guard holding the profile, per-run nonce, owned parent/integration ids, allowed normalized created/updated specifications, baseline inventory, and a package lease. Register both the prior and intended next state before an update, since an ambiguous result may leave either. It retains failed cleanup leases for Drop. Each attempt performs fresh exact reads; one-shot transport sends each mutation once.
- [ ] Model the package lease as `None`, `Claimed { version }`, `Owned { version }`, and `CleanupPending { version }`. Reserve `Claimed` before install. Promote to `Owned` only after decoded one-shot install success plus exact status and inventory proof of the transition from absence. An error or unreadable observation leaves an unconfirmed claim: observe it, block success when it cannot be resolved as unchanged baseline, and never uninstall from that state. Set `CleanupPending` before the one-shot delete; exact absence and restored inventory clear it. An ambiguous delete is observed without replay, even on Drop. Test a pre-attempt failure followed by someone else's install and a failed observation after a possibly committed install.
- [ ] Bind parent ownership to the exact id, name, namespace, allowed nonce-bearing description and normalized portable specification, empty monitoring, no platform/protected/agentless/environment state, active-space-only visibility, `agents == 0`, and readable attachments. Bind integration ownership to the exact id, name, namespace, allowed nonce-bearing description, one owned parent, exact system version, complete allowed inputs and other portable fields, true top-level enabled, no managed/environment/secret state, and active-space-only visibility. Reuse production normalization and secret classification rather than accepting arbitrary fields because the nonce matches. Bootstrap inputs may be captured only after a fresh exact read proves the complete marker identity and production export validates the materialized nonsecret configuration.
- [ ] Add offline safety tests before implementation: a different nonce is never deleted; missing or malformed ownership fields fail closed; unknown or nonzero agent counts block parent deletion; an attached parent sends no delete; integration failure blocks parent/package deletion; pre-existing system is never removed; a changed version is never removed; ambiguous create/delete uses exact observation without replay; package drift blocks success.
- [ ] Implement cleanup in dependency order. A package removal requires confirmed owned install, no remaining owned policy, no new unowned consumer, the exact installed version, and an inventory equal to baseline plus that coordinate. Observe the inventory after removal. Never include values or raw payloads in public progress.
- [ ] Begin the live contract with feature gating, setup when the controller's private setup flag is absent, the existing rule/content/triage baseline, and a clean Fleet snapshot. Prove standalone setup runs once and controller-child setup is skipped. Refuse markers before package installation. Recheck package inventory and exact version immediately before bootstrap or policy creates.
- [ ] Ensure system as specified. For a fresh install, require complete evidence of the exact resulting version and inventory; an unreadable or competing result is a cleanup failure, not permission to uninstall.
- [ ] Import a marker agent artifact with explicit id, empty monitoring, and nonce description. Check preview does not create, then apply, get, marker-filtered list, JSON/YAML export, default conflict, skip, changed overwrite, unchanged overwrite, and exact normalized round trip.
- [ ] Materialize system defaults with one nonce-owned bootstrap integration using `inputs: {}` through a direct one-shot create, as the recorder does. Export through production normalization to obtain the complete nonsecret input map. Delete and verify bootstrap absence and parent detachment before the actual import lifecycle. Register bootstrap ownership before its create.
- [ ] Exercise the same preview/apply/get/list/export/conflict/skip/overwrite/unchanged/round-trip lifecycle for the actual integration. Require exact parent ids and package coordinate.
- [ ] While attached, invoke agent delete and require client-side `conflict`. Pair this live proof with a wiremock CLI regression that proves zero requests to the parent-delete route; remote unchanged state alone is insufficient evidence of no request.
- [ ] Delete integration before parent. Re-import both exported artifacts in dependency order, require the same ids and contents, and delete both again. Finish Fleet cleanup, then the existing baseline audits. Classify cleanup failure ahead of ordinary contract failure.
- [ ] Run `cargo test --locked -p elasticctl --test live --jobs 2 -- --test-threads=4` and compile the exact ignored test without running it. Review the ownership and ambiguity paths with Sol before live execution.

### Task 4: Review, measure, and record evidence

- [ ] Run adversarial whole-branch Sol review. Resolve confirmed behavior defects with a failing regression and spec update. Record scope and results in `docs/reviews/v0.6.2.md`.
- [ ] Run formatting, locked Clippy, locked workspace tests, dependency policy, package contents, fixture privacy, and conformance-report validation. Cap builds at two jobs and tests at four threads.
- [ ] Load the required secret-handling skill before using live credentials. Use only the main checkout environment inside the matrix command process; redirect raw output to private logs.

```bash
CARGO_BUILD_JOBS=2 cargo xtask conformance-matrix \
  --report-dir docs/conformance/v0.6.2
```

- [ ] Require ten contract results per actual target version, no Fleet residue, exact package restoration, unchanged prebuilt counts, and self-managed containers/volumes removed. Do not create stand-in reports for missing measurements.
- [ ] Validate all three generated reports before committing them. Add findings, measured spec entries, and an unreleased 0.6.2 changelog entry. Scan new narrative evidence with the existing privacy patterns and review it for target/object identities and raw failures that pattern scans cannot prove absent. Mark completed tasks here. Leave version bump and release workflows to release preparation.
